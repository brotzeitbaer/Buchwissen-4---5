#include "character.hpp"


int berechnen(int strengh, int factor)
{
	
	return strengh = strengh + (strengh * factor) / 100;
	
}
float berechnen(float fspeed, float ffactor)
{

	return fspeed = fspeed + (fspeed * ffactor) / 100.0f;

}
